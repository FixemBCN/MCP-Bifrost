node_modules/
.env
.env.*
dist/
build/
*.log
.DS_Store

# Clau del worker — mai al repo
.env
*.key

# Calibration output — contains fragments of the target codebase
calibratge/resultats/
.bifrost/

__pycache__/
*.pyc

# Build output. `mcp_bifrost.egg-info/` was committed once and then
# regenerated by every build, so it arrived as a diff in unrelated commits
# and would conflict between anyone building at the same time.
*.egg-info/

# Worker credentials. The manual tells people to create this file; it must
# never be committable, in this repo or in any clone of it.
.bifrost.env
*.env
!.env.example
